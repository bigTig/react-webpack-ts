import { ProSettings } from '@ant-design/pro-components'

/**
 * @name
 */
const DefaultSettings: Partial<ProSettings> | undefined = {
  navTheme: 'light',
  layout: 'mix',
  contentWidth: 'Fluid',
  splitMenus: true,
  fixedHeader: true,
  fixSiderbar: true,
  colorWeak: true,
  siderMenuType: 'sub',
  colorPrimary: '#217BA0',
}

export default DefaultSettings
